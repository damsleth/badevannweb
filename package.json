{
  "name": "badevannweb",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm run dev --workspace api\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace api && npm run build --workspace web",
    "test": "npm run test --workspace api && npm run test --workspace web",
    "test:e2e": "npm run test:e2e --workspace web",
    "lint": "npm run lint --workspace api && npm run lint --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
